# Layered pull bank

Run `node tools/GenerateLayeredPull/generate.cjs` from the repository.
Prepared source input from the selected 2026-09-09 revision 3 comparison.
Revision 4 moves body/limb contours with signed silhouette correspondence;
source outline colors are transported to matching contour depth before blending.
Spatial steps are bounded and backtracked to prevent out-of-image lookups.
The far hindpaw has its own hip/tip motion and is drawn behind trunk and forelegs.
Output: 113 sequential 96x96 Pbgra32 rasters, gzip compressed.
Eight original keys remain exact. Product tests compare representative frames to
the selected browser output. Runtime interpolates only adjacent dense frames;
head registration follows the original key landmarks continuously.

Run `node tools/GenerateLayeredPull/verify-contours.cjs` for single-contour,
opaque-interior, paw ownership and escaped-color regressions. Run
`node tools/GenerateLayeredPull/verify.cjs --bank` for all-frame bank parity,
original keys, both-side key continuity, reverse playback, rear joint motion and
head preservation. `head-reference.json` freezes 113 isolated head hashes from
the prior revision 3 comparison before body edits; it is not regenerated by the
production generator. Raster output is regenerated offline, not on the UI thread.

The former half-transparent body/foot duplicates at 25/41/57 are addressed.
This remains low-resolution authored raster art, not a resolution-independent
vector redraw. Actual desktop feel is additionally checked through user trials.
