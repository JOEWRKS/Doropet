# Selected interaction checkpoint: ordinary rump pull

The user compared the saved ordinary-rump and dangling-rump desktop builds, preferred ordinary pulling for now, and explicitly requested a commit and push. This checkpoint selects the historical ordinary build, not a new combination of later hanging changes.

## Included behavior

The approved rounded dynamic cheek/face deformation, expanded cheek hit area, explicit head/body routing, three interactive paws plus belly/rump regions, supplied head-drag frames, physical-speed head swing and head drop/squash remain included. Rump interaction uses local stretch/carry and returns normally on release. Rump hanging, head-down rotation, paw-gravity warp, later stronger rump stretch and rump-specific drop/rebound are not included.

## Authority and verification

All254 source/test/tool files match the archived `cheek-hit-routing-20260907-attempt-1/source-after.json` authority. Six shared files were restored from the byte-matching pre-hanging snapshot. Twenty hanging-only files were moved out of build inputs into a recoverable local archive, not permanently deleted. The complete preceding274-file source snapshot and both saved runtime packages remain available locally.

Fresh Release tests: App354/354 and Core96/96. The rebuilt App DLL is byte-identical to the user's saved ordinary executable package on this machine:

`FDD595114C7E044877CF259DCBBF8B0B9FC63942F19C2FC5979597B3030137AC`

Core DLL:

`7032D940E5314DE2409E416478D64FD68F666B1DB413612AB708D5518B290365`

Runtime-composition, direct-interaction-render, exact-art and approved13-asset scripts each exit0. The frozen `ApprovedSurroundingPullRenderer` test reference retains its existing final blank line for exact identity; this is the sole default staged whitespace warning. No product XAML EOL cleanup was performed.

Local verification records are under `artifacts/repro/plain-rump-checkpoint-20260907`. The ordinary and dangling runtime folders/ZIPs remain under `Downloads/doro/saved-versions-20260907`. This checkpoint does not replace either running desktop instance. No PR, merge, worktree cleanup, or forced push is part of the request.
