# SDD ledger — plan: docs/plans/2026-08-31-dororong-direct-interactions.md

Workspace: `D:/JOEWRKS/.worktrees/DororongDesktopPet-m1-expression-animation`
Branch: `feature/dororong-m1-expression-animation`
Isolation: linked git worktree (`git-dir` differs from `git-common-dir`; no superproject)
Spec: `docs/specs/2026-08-31-dororong-direct-interaction-design.md`
Plan checkpoint: `8bbe23e`

## Pre-flight task/interface scan

| Task(s) | Producer / consumer or own-text check | Finding |
|---|---|---|
| 1 | Own text: existing attempt-38 diff → fresh tests → product commit → evidence/docs commit | Internally consistent. Title says accepted checkpoint, but steps correctly preserve the user's exact `나쁘지 않다` wording and prohibit promotion of Stage A/M1. |
| 2 | Own text: RED app tests → immutable types/controller → GREEN/build → focused commit | Internally consistent; no product input routing or art is claimed. |
| 3 | Own text: RED target/core/loop tests → alpha classification → core bit → controller/capture integration | Internally consistent after plan self-review added the autonomous-loop early return. |
| 4 | Own text: RED sequence tests → focused utility → attempt-38-equivalent refactor | Internally consistent; existing rendered alpha values remain the regression authority. |
| 5 | Own text: asset RED → one coherent nine-panel body-click family → root review → user approval → promotion | Internally consistent; integration is explicitly forbidden before approval. |
| 6 | Own text: WPF RED → sequence mapping → rendered/product verification → one Windows attempt | Internally consistent and consumes only approved Task 5 bytes. |
| 7 | Own text: asset RED → one coherent twelve-panel sketch-derived drag family → root/user review → promotion | Internally consistent; sketch controls silhouette, canonical controls identity. |
| 8 | Own text: RED → drag phase mapping → full verification → one Windows attempt | Internally consistent; pointer/window response is not delayed by art entry. |
| 9 | Own text: per-side asset RED → coherent per-side sheets → root/user approval → promotion | Internally consistent; approval is side-specific. |
| 10 | Own text: RED → side/strength/release mapping → verification → ordered Windows observation | Internally consistent; no body state/window movement is allowed. |
| 11 | Own text: complete fresh suite → root visual check → ordered Windows checks → truthful verdict/handoff | Internally consistent; overall M1 is not automatically promoted. |
| 1 → 4 | Task 1 preserves attempt-38 presenter/test; Task 4 extracts its blender | Compatible: Task 4 must keep Task 1 exact endpoint/alpha evidence. |
| 1 → 11 | Task 1 writes TASKS/roadmap boundary; Task 11 finalizes the same ledger docs | Compatible: Task 11 appends current evidence without rewriting historical verdicts. |
| 2 → 3 | Task 2 produces target/snapshot/controller; Task 3 routes them through WPF/core/loop | Compatible signatures after plan type review. |
| 2 → 8 | Task 2 produces drag entry/settle progress; Task 8 renders it | Compatible; art begins only after core reports `DRAGGED`. |
| 2 → 10 | Task 2 produces cheek strength/release; Task 10 renders it | Compatible; controller stores locked outward sign and release strength. |
| 3 → 4 | Task 3 changes presenter render signature; Task 4 refactors frame sampling in that presenter | Compatible sequential edits; Task 4 must retain the combined render contract. |
| 3 → 6 | Task 3 produces pending body/direct snapshot; Task 6 maps pending/click presentation | Compatible; core remains the click confirmation authority. |
| 3 → 8 | Task 3 preserves grab offset/capture; Task 8 adds visual entry/settle | Compatible; window movement remains core/loop-owned. |
| 3 → 10 | Task 3 produces mirror-aware locked cheek target; Task 10 maps the approved side family | Compatible, subject to the cheek-region ruling below. |
| 4 → 6 | Task 4 produces `PremultipliedFrameSequence`; Task 6 consumes it for body click | Compatible. |
| 4 → 8 | Task 4 sequence utility → drag entry/settle | Compatible. |
| 4 → 10 | Task 4 sequence utility → cheek pull/release | Compatible. |
| 5 → 6 | Task 5 exact approved body-click assets → Task 6 integration | Compatible; hashes and order are acceptance identity. |
| 5 → 7 | Task 5 creates shared asset test/csproj entries; Task 7 extends them | Compatible additive change. |
| 5 → 9 | Task 5 creates shared asset test/csproj entries; Task 9 extends them | Compatible additive change. |
| 6 → 8 | Task 6 creates direct-render test/presenter mapping; Task 8 extends both | Compatible additive state mapping. |
| 6 → 10 | Task 6 creates direct-render test/presenter mapping; Task 10 extends both | Compatible additive state mapping. |
| 7 → 8 | Task 7 exact approved drag assets → Task 8 integration | Compatible; exact art identity is preserved. |
| 7 → 9 | Task 7 extends shared asset test/csproj; Task 9 extends them again | Compatible additive change. |
| 8 → 10 | Task 8 presenter/controller tests → Task 10 cheek extensions | Compatible; body and cheek routes remain target-locked. |
| 9 → 10 | Task 9 exact approved per-side assets → Task 10 integration | Compatible; per-side approval is required. |
| 5/7/9 → 11 | Approved family hashes/render packages → final visual identity check | Compatible; final check cannot substitute for user Windows observation. |
| 6/8/10 → 11 | Per-slice Windows evidence → final non-interference matrix | Compatible; individual rows retain their own verdict. |

## Pre-flight rulings

Ruling: The word “accepted” in Task 1's title means only “acceptable checkpoint from which the user chose to continue”; the evidence must quote `나쁘지 않다` and must not label all Stage A or M1 PASS — the spec's explicit boundary controls — if wrong, the cost is a docs-only verdict correction.

Ruling: Task 3's cheek rectangles are initial canonical-space test anchors, not immutable visual authority. The spec requires a bounded region on the actually visible cheek, so root visual calibration may adjust those constants before integration while retaining mirror/anatomical tests — if wrong, the cost is redoing descriptor tests and hit-region calibration before cheek art integration.

Ruling: For Tasks 5, 7, and 9, an evidence-based imagegen edit is still the primary method, not a materially different fallback. If it still fails identity/continuity, the one fallback is local neighbor-preserving raster correction from approved adjacent frames; if that also fails, stop that candidate for user direction — if wrong, the cost is discarding one candidate family, never silently promoting drift.

Ruling: SDD continuous execution proceeds through code/test tasks, but exact art-family approval is a binding product gate from the spec. The controller will stop at Tasks 5/7/9 approval points instead of integrating unapproved art — if wrong, the cost is schedule delay rather than unauthorized product assets.

Task 1: review warning resolved — slow/fast/drag wake routes are intentionally outside the attempt-38 checkpoint and remain `UNVERIFIED`, exactly as the brief/spec require.
Task 1: complete (commits 8bbe23e..ced60f8, review clean)
Task 2: minor (deferred): the cheek clamp/damping test saturates horizontally, so it does not independently prove vertical damping or the precise clamp threshold; final review must decide whether to strengthen it before merge.
Task 2: fix round 1/5 (1 addressed, 0 open — active target can no longer be replaced by a second Begin; commits 29d4704..48a6230)
Task 2: complete (commits ced60f8..48a6230, review clean; 1 deferred minor)
Task 3: Ruling: immediate body pointer-down wake from `SLEEP` is required by spec §3.2 (“pointer-down on any valid target wakes immediately”) and preserves the earlier user-approved click-only wake behavior; keep the new body-down wake — if wrong, the cost is changing wake timing back before final Windows body-click observation.
Task 3: Ruling: `BodyDragSettle` remains presentation-local direct-interaction metadata and suppresses proximity/autonomy for its bounded 180 ms so a reaction cannot replace the settle before the existing wakeful baseline; keep `LocalInteractionActive` during settle — if wrong, the cost is at most 180 ms of delayed autonomous/proximity response after drag release.
Task 3: fix round 1/5 (1 addressed, 0 open — active frame/transition descriptors and actual visible mirroring now govern target classification; commits 004934c..87efffe)
Task 3: complete (commits 48a6230..87efffe, review clean; 2 plan/spec rulings retained)
Task 4: minor (deferred): smoothstep regression test samples only local 0.5, where smoothstep equals linear; final review must decide whether to add a non-midpoint sample.
Task 4: minor (deferred): dimension rejection also changes stride, so equal-stride/different-height validation is not independently proven.
Task 4: minor (deferred): `PremultipliedFrameSequence` retains the caller's mutable `IReadOnlyList`; snapshotting would make construction-time validation durable.
Task 4: complete (commits 87efffe..2ba95c3, review clean; 3 deferred minors)
Task 5: blocked before approval/integration — built-in imagegen attempt and its one targeted edit rasterized the checkerboard, redrew protected identity, and lacked four readable airborne legs; the materially different local canonical-preserving fallback still has spike-like center-leg anatomy, an apex/dangle→fall continuity jump, 159 changed recovery pixels, and a 9-frame GIF whose decoded delays are all 0 ms. Root independently inspected attempts 1/2 plus native/4x/onion/difference evidence and independently decoded `FRAMES=9`, `DELAYS_CS=0,0,0,0,0,0,0,0,0`, `TOTAL_MS=0`. No product assets/csproj/commit/approval handoff occurred. New art method requires user direction under the bounded-attempt rule.
Task 5: user direction received — user selected option 1, authorizing one new `body-click-v2` method: preserve exact canonical/protected pixels and redraw only the lower body/four-leg motion sequentially from neighboring frames. `body-click-v1` remains immutable failed evidence; v2 must use a new candidate path and still cannot enter product assets before root visual PASS and exact-family user approval.
Task 5: execution was user-interrupted after immutable v2 frame creation and before packaging; no product promotion occurred. User then said `다시`, authorizing resume from the existing exact v2 bytes. Resume begins with packaging/visual verification and must not regenerate or overwrite those frames.
Task 5: v2 technical packaging PASS / root visible-appearance FAIL — exact recovery/canonical hash, zero protected mismatches, four-leg row readback, and 9-frame 500 ms GIF pass, but nearest-4x inspection shows rise/apex/dangle/fall legs as four parallel rectangular tooth/comb columns rather than rounded natural dangling legs. Preserve v2 bytes. Causal hypothesis: the fallback's hard vertical leg columns plus flat lower-body join create the tooth-like reading. One targeted v3 correction is authorized within the user-selected exact-canonical/lower-body method: rounded/tapered lobes, curved U valleys, staggered lengths, and continuous retraction; no other region changes.

Task 5: complete (commits 2ba95c3..c78ee78) — all generated/procedural body-click pose candidates were rejected and preserved as evidence only; the plan/spec switched the product slice to exact canonical artwork plus whole-character transforms, with no candidate art promotion.
Task 6: complete for implementation (commits c78ee78..1f31be3, review approved with minor) — transform-only body click, facing/cancel/wake regressions, canonical-eye correction, and attempt-2 evidence are checkpointed. Actual Windows canonical-eye preservation PASS; pending-press feel, focus preservation, and transparent click-through remain UNVERIFIED and stay in Task 11 acceptance.

Ruling: The user's 2026-09-01 overnight instruction explicitly supersedes the earlier Tasks 7/9 stop-before-integration ruling for this branch-only run. Produce and integrate provisional complete-character drag and cheek families so the full interaction set is executable by morning, but label those families `PROVISIONAL`/`UNVERIFIED`, keep exact candidate/evidence bytes, and do not push or merge — if wrong, the cost is discarding or replacing the provisional art and its presentation mapping before any release.

Task 7 visual contract frozen before candidate inspection: canonical invariants are (1) unchanged head/hair silhouette and top anchor, (2) unchanged face placement and open-eye identity, (3) unchanged rose/bow/ribbon order and connectivity, (4) single-weight dark outline around a no-tail white body, and (5) four readable legs with no extra protrusion. Drag-specific variation is limited to continuous downward torso extension from the fixed grab/top anchor, four separated hanging legs, and a coherent return toward canonical. Evidence layers remain separate: asset identity/geometry, rendered motion, runtime interaction, and user acceptance.

Task 7 review: spec/quality FAIL. Root and reviewer both observed the same objective defect in attempt 1: canonical-to-entry-01 and settle-03-to-canonical change the rounded lower body into/out of a tall rectangular comb-like four-leg topology in one frame (1,135/1,188 changed pixels versus 358–633 internally). The original attempt-1 evidence and commits remain preserved.
Task 7: Ruling: reviewer Critical “product promotion without user approval” is plan-mandated but superseded by the later explicit overnight user instruction recorded above; keep provisional branch-only product staging and `UNVERIFIED` labels, with no push/merge — if wrong, the cost is reverting the provisional resource/assets before release. The objective anatomy-jump findings remain load-bearing and enter fix round 1.
Task 7: fix round 1/5 (all scoped objective findings addressed — attempt 2 replaces the rectangular/equal-tip comb with a continuous rounded deformation, strengthens endpoint/topology regression checks, and binds an ordered positive-delay 800 ms playback; commits 3332be1..c6c906f).
Task 7: complete for provisional implementation input (commits 1f31be3..c6c906f, scoped re-review PASS). Root directly inspected the exact attempt-2 native/4x/onion/difference strips and found the protected head/face/ornaments preserved and the lower-body deformation continuous enough to integrate under the overnight ruling. Runtime motion feel and user acceptance remain `UNVERIFIED`; no release approval is implied.
Task 8: complete (commits c6c906f..df9669f, independent review PASS) — seven entry keys, exact full-hang hold, five settle keys, same-tick pointer/window movement, original grab offset, release clamp, capture release, locked facing, one complete-character `Pbgra32` surface, and canonical recovery are deterministically covered. Actual Windows motion feel, focus preservation, transparent click-through, and user acceptance remain `UNVERIFIED`. A broad historical PowerShell sweep encountered the pre-existing `Dororong.App.BlinkRecovery.Tests.ps1` direct call to the now-internal presenter `Render`; Task 8-relevant asset/render suites passed, and the unrelated harness was neither retried nor changed.

Task 9 visual contract frozen before candidate generation: each side is a separate complete-character family with six pull keys (rest/press/quarter/half/three-quarter/full) and five release keys. The canonical head/hair/top anchor, eyes, mouth, rose/bow/ribbons, body, opposite cheek, and all unrelated outlines stay pixel-stable; only the selected anatomical cheek's skin/contour may extend outward, with a very small adjacent face response. Pull direction is horizontal and side-specific, vertical response is damped, the full-pull contour remains rounded and attached, and release returns byte-exactly to canonical after at most one restrained overshoot. No face-wide skew, cheek swap, eye drift, hair tear, overlay, ghost, or window-anchor movement is allowed.
Task 9: Ruling: the brief's phrase “alpha silhouette stay stable” cannot literally include the selected cheek because the same brief/spec requires that cheek to extend outward. Treat it as “all non-target alpha silhouette stays stable; selected-cheek contour alone may change” — if wrong, the cost is replacing the provisional cheek families and their mapping before release, with no effect on the accepted canonical asset.
Task 9 attempt 1: both the primary imagegen sheets and the first deterministic fallback were rejected before promotion. Imagegen baked an opaque checkerboard and redrew scale/body/legs/identity; the fallback preserved protected bytes but 12x inspection showed only the viewer-right long hair/body boundary or viewer-left pink hair rim moving, not a readable cheek. Exact rejected bytes remain immutable; no product resources or commits were created, and the asset test remains RED on missing keys.
Task 9: Ruling from new visual evidence: at 96x the selected cheek contour is partly occluded by its adjacent side-hair tip, so requiring every adjacent hair pixel to remain fixed makes an outward cheek pull visually unreadable. Authorize one final, materially different recovery method: redraw the selected lower-face lobe as a rounded attached skin contour and let only the immediately touching side-hair tip follow by a few pixels; hair roots, bangs, eyes, mouth, opposite cheek, ornaments, body, and all other pixels remain exact. This is still provisional and must be rejected if 12x inspection reads as hair-only movement or identity drift — if wrong, the cost is discarding this recovery family and mapping before release.
Task 9: blocked after the single evidence-based recovery attempt. The strengthened candidate test correctly REDs attempt 1 (`Left ... selected-cheek skin: 0 pixels`). Attempt 2 failed its first GREEN at 42 hair-involved pixels versus the frozen 40-pixel tip-only bound; root and implementer independently inspected native/4x/12x evidence and found the viewer-right lobe reads as a detached paw/ribbon over the body while the viewer-left contour has checker-like dark artifacts. No retry, product promotion, resource entry, presenter integration, app launch, publish, or commit occurred. Rejected attempts remain immutable evidence, the temporary RED test was restored to committed HEAD, and cheek art/runtime/user acceptance remain `UNVERIFIED`/not delivered.
Task 10: not started because it consumes exact Task 9 cheek families and none passed the visual contract. Integrating the controller against rejected art or substituting an unplanned transform would misrepresent completion; cheek press/pull/release remains logic-only groundwork with no product presentation mapping.
Task 11: truthful partial checkpoint recorded (commits df9669f..c872164) — legacy presenter harnesses were migrated without product changes; separate Core 82/82, App 53/53, five focused harnesses, deterministic click/drag renders, Release build, publish/process identity, and blocked cheek evidence were recorded. The final complete PowerShell loop remains `UNVERIFIED` because its invocation-bound final exit was lost; body click canonical eyes retain their historical user-observed PASS, body-drag art/runtime feel remains `PROVISIONAL / UNVERIFIED`, cheeks remain not delivered, all unobserved Windows rows remain `UNVERIFIED`, and overall M1 remains `PARTIAL`.
Task 11 final-review ruling: held body drag must apply the current work-area/pet-size clamp on the threshold-crossing tick and every subsequent held tick while preserving the original grab offset; release-only clamping is insufficient because it permits visible escape and snap-back. `BodyPending` does not own capture, cheek phases declare local capture, core `DRAGGED` owns body capture, and settle/release own none — if wrong, the cost is revisiting only drag-boundary/capture ownership before Windows acceptance, not changing presenter art or timing.
Task 11 final-review fix: complete at `8fa1a96385025f775abf57d4f72a7c77a1586912` — evidence-bound RED/GREEN checks pass, fresh Core 86/86 and App 53/53 pass, Release build is clean, and scoped re-review reports PASS with no new findings. Deterministic held-drag work-area bounds and capture metadata consistency are PASS; actual Windows boundary feel/capture cleanup remain `UNVERIFIED`. The current attempt-3 runtime is immutable at `artifacts/repro/direct-interactions-overnight-attempt-3/runtime/` (exe `4AFC145876F2F3CBC5C15D450655E3E5EC966DB9BECA3D4017A3D53BB771AE3A`) and was launched once as PID 47088; process liveness does not promote any Windows row.
